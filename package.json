{
  "name": "syntheverse-genesis-base-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "Syntheverse Genesis artifacts (SYNTH 90T Motherlode + Lens/Sandbox/Kernel) deployed via Hardhat to Base.",
  "scripts": {
    "build": "hardhat compile",
    "compile": "hardhat compile",
    "deploy:base": "hardhat run --network base scripts/deploy-genesis.ts",
    "deploy:baseSepolia": "hardhat run --network baseSepolia scripts/deploy-genesis.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "@nomicfoundation/hardhat-verify": "^2.1.0",
    "@types/node": "^22.10.2",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.18",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "ethers": "^6.13.4"
  }
}


